import { useI18n } from "../shared/i18n/I18nProvider";
import { Link, useLocation } from "react-router-dom";
export function Breadcrumbs(){const {t}=useI18n();const parts=useLocation().pathname.split("/").filter(Boolean);return <nav className="breadcrumbs" aria-label={t("Ruta")}><Link to="/dashboard">Console</Link>{parts.map((part,index)=>{const path=`/${parts.slice(0,index+1).join("/")}`;const names:Record<string,string>={"criteria-filters":"Criterios y Filtros",customers:"Cliente",middleware:"Middleware",blackouts:"Blackouts","inventory-services":"Inventory Services",policies:"Policies","aiops-extensions":"AIOps Extensions","auto-suppression":"AutoSuppression",administration:"Administración",ess:"Estado de eventos · ESS",dashboard:"Dashboard general",events:"Eventos",correlation:"Correlación",automations:"Automatizaciones"};const label=t(names[part]??part);return <span key={path}><b>/</b>{index===parts.length-1?<em>{label}</em>:<Link to={path}>{label}</Link>}</span>})}</nav>}
