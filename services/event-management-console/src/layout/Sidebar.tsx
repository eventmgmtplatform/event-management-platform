import { useI18n } from "../shared/i18n/I18nProvider";
import { NavLink } from "react-router-dom";
import { NavigationIcon } from "../navigation/NavigationIcon";
import { navigation } from "../navigation/navigation.registry";

export function Sidebar({open,onClose}:{open:boolean;onClose:()=>void}){const {t}=useI18n();return <aside className={open?"sidebar open":"sidebar"} aria-label={t("Navegación principal")}><div className="brand"><span className="brand-mark">EM</span><div><strong>Event Management</strong><small>Console 1.0.0</small></div><button className="icon-button mobile-only" onClick={onClose} aria-label={t("Cerrar menú")}>×</button></div><nav>{navigation.map(group=><section className="nav-group" key={group.label}><span className="nav-group-title">{t(group.label)}</span>{group.items.map(item=><NavLink key={item.path} to={item.path} title={t(item.description)} onClick={onClose} className={({isActive})=>isActive?"nav-item active":"nav-item"}><NavigationIcon name={item.icon}/><span>{t(item.label)}</span>{item.status==="preview"&&<span className="nav-dot" title={t("Próximamente")}/>}</NavLink>)}</section>)}</nav><footer className="sidebar-footer"><span className="status-dot"/>{t("Console local")}<small>{t("Fuente de datos indicada en cada módulo")}</small></footer></aside>}
